# Token and batch-context intervention — frozen 2026-09-21
CPU only, two threads. No optimizer updates or new training. Instantiate the actual 3000-input, 8-token, width-128, two-layer, four-head attention-topic model with seeds 0,1,2; save initial states. Use first 64 stored Setty held-out cells with original normalized features. The inputs are public single-cell data, but the intervention tests current untrained computation only.
Compare mean deterministic allocation after softmax(mu) for: unchanged tokens; reversal of complete projected+embedded tokens; projecting all tokens to their per-cell mean before attention; duplicating the first complete token. Also compare each of first 16 cells in isolation, an ordinary 64-cell batch, a batch with other cells' normalized features multiplied by 10, and reversed batch order. Evaluate in eval mode and train mode with dropout configured zero, without stochastic reparameterization. Predeclare permutation/batch invariance tolerance max absolute allocation difference <=1e-5; mean-token and duplicate-token sensitivity is descriptive with all directions retained. This can falsify cross-cell computation or token insensitivity, not identify historical collapse causes. Save model/input hashes, per-cell allocations and per-seed results. Estimated CPU 3–8 minutes.

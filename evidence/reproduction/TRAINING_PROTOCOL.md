# Matched concentration intervention — frozen 2026-09-21 before training
Setty and Dentate, seeds 0,1,2, concentrations 0.1 and 1.0. Twelve arms. K=10, width128, eight tokens, four heads, two layers, FF256, dropout0; original multinomial reconstruction + 0.01 Dirichlet-derived KL with per-coordinate free-bit threshold0.1. Same autoencoder initialization within each concentration pair; concentration-dependent topic prior buffers intentionally differ. Word prior fixed at ones; these buffers do not enter the implemented loss. Hash all states.
Each arm completes 200 epochs, 16 complete 128-cell batches/epoch, 3200 AdamW updates, 409600 presentations, learning rate and weight decay0.001, gradient norm clip10. No early stopping, scheduler, AMP, accumulation or selection. Pair matches batch order and per-step torch RNG seeds. Train/unused-validation/test split and preprocessing are frozen legacy inputs, 2100/450/450 and 3000 genes; this is not train-only feature selection. Export deterministic softmax(mu) native allocations for all 450 test cells.
Primary endpoint: allocation collapse status, requiring all test cells share argmax and max coordinate range<=1e-8. Also report mean perplexity, largest coordinate range, argmax occupancy and exact distinct-pair Jensen-Shannon mean. A denominator<=1e-12 is flagged degenerate independently of the collapse definition. Do not choose new cutoffs after observing results. Setty 5-fold shuffled(seed42) 15-neighbor uniform regression of stored branch probabilities is auxiliary; degenerate allocation scores withheld. Retain all twelve arms and loss traces, even no collapse. No hypothesis test or biological-replication claim.
On saved trained final states, run the previously frozen token and batch-context probes for all arms, eval and train(dropout0) modes, without optimizer updates. The initial-model protocol is superseded for the primary mechanism evidence by these trained-state probes. Each probe acts on actual tensor [batch,8,128] after learned projection and token embeddings, before the transformer layers. Use the included fixed input files and unchanged model snapshot. CPU computations use at most two threads. The distributed protocol omits execution-scheduling notes; scientific design and historical computation hashes are retained.
